import { AuthLayout } from "@/app/components/templates/Auth";
const LoginPage = () => {
  return (
    <AuthLayout>
      <h1 className="text-xl font-semibold text-center">Login form</h1>
      {/* Form will be added later */}
    </AuthLayout>
  );
};

export default LoginPage;
